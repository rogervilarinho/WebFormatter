@model WebAffinitiesMVC.Models.LISTA

@{
    ViewBag.Title = String.Concat("Exclusão de Lista");
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
    <div class="page-header">
        <h2 id="tables">@String.Concat("Exclusão de Lista")</h2>
    </div>
</div>
<div class="container">
    <div class="col-lg-8">
        <h3>@String.Concat("Deseja excluir a lista ", Model.NOME, "?")</h3>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.NOME)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NOME)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DESCRICAO)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DESCRICAO)
            </dd>

            <dt>LAYOUTs
                @Html.DisplayNameFor(model => model.ID_ARQUIVO)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ARQUIVO.NOME)
            </dd>

        </dl>
    </div>
    <div class="col-lg-4 col-lg-offset-0">
        <div class="alert alert-dismissable alert-danger">
            <button type="button" class="close" data-dismiss="alert">×</button>
            <strong>Atenção!</strong> <a class="alert-link">A exclusão da lista</a> excluirá também todos os valores associados a mesma.
        </div>
    </div>
</div>
<div class="container">
    <div class="col-lg-12">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayName("Valores da lista")
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.LISTADETALHE)
                {
                    <tr class="active">
                        <td>
                            @Html.DisplayFor(modelItem => item.VALOR)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<div class="container">
    <div class="col-lg-3">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="bs-component">
                @Html.ActionLink("Cancelar", "Index", "Listas", new { idArquivo = Model.ID_ARQUIVO }, new { @class = "btn btn-default btn-sm" }) |
                <button type="submit" class="btn btn-primary btn-sm" value="Delete" data-toggle="tooltip" data-placement="right" title="" data-original-title="Todos os valores da lista serão excluídos">Excluir</button>
            </div>
        }
    </div>
</div>